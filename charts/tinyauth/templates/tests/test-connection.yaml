apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tinyauth.fullname" . }}-test-connection"
  labels:
    {{- include "tinyauth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tinyauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
