apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yamtrack.fullname" . }}-test-connection"
  labels:
    {{- include "yamtrack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yamtrack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
